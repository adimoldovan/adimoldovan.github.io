document.addEventListener('DOMContentLoaded', function() {
    const mainContent = document.getElementById('content');
    mainContent.innerHTML = '<p>This is a simple static site built with vanilla JavaScript, HTML, and CSS.</p>';
});